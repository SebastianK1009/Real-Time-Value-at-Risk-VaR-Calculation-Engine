apiVersion: v1
kind: Service
metadata:
  name: {{ include "risk-stream-processor.fullname" . }}
  labels:
    {{- include "risk-stream-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "risk-stream-processor.selectorLabels" . | nindent 4 }}
